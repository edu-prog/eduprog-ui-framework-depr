const BreakpointsLabel = ["xs", "sm", "md", "lg", "xl"];

interface ISizes {
  xs: string;
  sm: string;
  md: string;
  lg: string;
  xl: string;
  fluid: string;
}
export { BreakpointsLabel, ISizes };
